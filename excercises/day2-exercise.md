# Day 2 Exercise — Task Class

## Task
Create a `Task` class with title, status, and a display method.

## Instructions
1. Define a class `Task` with:
   - `__init__` method accepting `title` and `status`
   - A `display()` method that prints the task details
2. Create at least 3 task objects and call `display()` on each

## Starter Code
```python
class Task:
    def __init__(self, title, status):
        # Your code here
        pass

    def display(self):
        # Your code here — print something like: [pending] Buy groceries
        pass

# Test it
t1 = Task("Buy groceries", "pending")
t2 = Task("Fix bug", "in-progress")
t3 = Task("Write report", "done")

t1.display()
t2.display()
t3.display()
```

## Expected Output
```
[pending] Buy groceries
[in-progress] Fix bug
[done] Write report
```

## Your Solution
_(write your solution below)_

```python

```
